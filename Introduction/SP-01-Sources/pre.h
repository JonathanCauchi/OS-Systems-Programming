#define MAX(X,Y) ((X) > (Y) ? (X) : (Y))
#define MIN(X,Y) ((X) < (Y) ? (X) : (Y))

#define ONE 1
#define TWO 2

int add_two(int a, int b) {
  return a + b;
}

int sub_two(int a, int b) {
  return a - b;
}